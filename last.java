import java.util.*;
class last
{
	public static void main(String ar[])
	{
		int i;
		Scanner s=new Scanner(System.in);
		ArrayList <String> list1= new ArrayList<String>();
		int n=s.nextInt();
		for(i=1;i<=n;i++)
		list.add(s.nextString);
		for(list.hasNext())
		String str=list.next()
		char c=str.chatAt(str.length());
		
	}
}